DEV ENVIRONMENT:
1. create a file named "settings_dev.py" in the top level directory
2. set up database you are using locally
3. set the absolute path to the template directory and static file directory


DATABASE SETUP:
1. create a new mysql database with the name "track_analyzer"
